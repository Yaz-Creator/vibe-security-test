# Copy this file to .env and fill in real values. Never commit .env to git.
API_KEY=your-api-key-here
ADMIN_PASSWORD_HASH=your-bcrypt-hash-here   # generate with: node -e "require('bcrypt').hash('yourpassword',12).then(console.log)"
PORT=3000
